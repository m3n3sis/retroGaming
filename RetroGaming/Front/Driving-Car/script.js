$(document).ready(function() {

    // variables

    $surface = $('.surface');
    $car = $('car');
    $img = $('.car img');
    let flag = true;


    // déplacement
    $($surface).toggleClass('moveRight');
});